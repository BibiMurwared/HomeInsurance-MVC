/*
appsettings.json

This is your configuration file.

Later it will hold:

SQL Server connection string
logging settings

Why it matters:

configuration should not be hardcoded in classes

*/
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
